https://www.sejuku.net/blog/82822
typescript -- jsファイルに、しっかりと型を定義したもの(しないとエラーになる)
tsc     ---- typescriptファイルをjsファイルに変換
node    ---- jsファイルを実行

typescript の覚えるポイント
型
boolean  真偽値    
number   数字          
string   文字列  
any  なんでもいい(基本的に使用禁止)
void なにもなし
変数設定
let a: boolean = true;
関数設定
function func(a: number, b: boolean) : void{
    console.log('hello')
}
配列設定
let aa: number[] = [10, 20]
オブジェクト設定
interface Hoge{
    a: number,
    b: number,         b?: number とすると、bは設定してもしなくてもよい変数になるundefined
    c: number          c: number | null   cがないときに、nullと設定することができるnullable
}
let v: Hoge = {a:10, b:20, c:30}





Vue 最終課題(sassなどもある)
https://github.com/gwenf/vue3-fcc-course-basic-product-cart-demo 







